const { MongoClient } = require("mongodb");
const uri = process.env.mongo_uri;

//would contain the model/singleton 

async function search(item, req, res){
     try {
    const database = client.db('cmps4150');
    const parts = database.collection('tasks');

    // Hardwired Query for a part that has partID '12345'
    // const query = { partID: '12345' };
    // But we will use the parameter provided with the route: URL/api/mongo/12345
    
    const query = { partID: req.params.item };

    const part = await parts.findOne(query);
    console.log(part);
    res.send('Found this: ' + JSON.stringify(part));  //Use stringify to print a json

  } finally {
    // Ensures that the client will close when you finish/error
    await client.close();
  }
}